module.exports = {
	APP_PORT: 8080, // port which server will use for hosting data
	host: 'localhost', // variables which used to get connection to MongoDB
	db_port: 27017,
	db_name: 'dataBase',
	allowedOrigins: ['http://localhost:3000'], // origins which are able to make requests to server
};
